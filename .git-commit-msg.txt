fix: remove stale code + fix progress API position bug + lint errors

Bugs fixed:
- Progress API: `pos || existing.position` treated position:0 as "not sent",
  so handleEnded's position:0 reset was silently ignored. Now tracks
  whether position was explicitly provided via `hasPosition` flag.
- handleToggleComplete: only reverted on network error (catch), not on
  HTTP error (401/500). Now checks `res.ok` and reverts on any failure.
  Also preserves the original progress object for clean revert.
- Track-change effect: synchronous setState calls caused cascading renders
  (react-hooks/set-state-in-effect). Now deferred via Promise.resolve().

Stale code removed:
- TalksClient: removed unused `useRef` and `CheckCircle2` imports.
- TalksClient: removed dead `?? idx` fallback (sortOrder is always a number).
- audio-player-context: removed unused `CachedTalkDownload` import.
- process route: removed unused `useFastPath` variable (recomputed as `useFast`).
- audio process: removed unused `probe` variable assignment.
- AdminTalks: escaped quotes in JSX text (pre-existing lint error).
- fun-fact-popup: deferred setState in effect (pre-existing lint error).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
