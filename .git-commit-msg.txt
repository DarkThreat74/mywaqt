fix: talks page crash (missing talk_progress table) + mini player minimize button

1. Talks page crash — root cause: missing database tables
   The talk_progress table and talk_folders.folder_color column were
   defined in schema and migration 0021 but never applied to the Neon
   database. The /api/talks GET route queries talk_progress on every
   load, causing a 500 error and "something went wrong" on the talks page.
   Applied migration 0021 to production Neon database via MCP.

2. Mini player minimize button
   The mini bar had no way to go back to the bubble (FAB) view — only
   expand to full or close entirely. Added a Bubble button (ChevronDown)
   next to the Close button that minimizes the mini bar back to the
   floating circle.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
