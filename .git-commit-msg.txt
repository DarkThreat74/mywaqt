feat: batch upload — step-based UI, drag-drop file list, start number selector

Redesigned the batch upload form into a clear 3-step flow:

Step 1 — Select audio folder:
- Empty state: large dashed dropzone with icon + "Choose folder"
- After selection: summary card with green file count badge, total size,
  "Clear all" button, scrollable drag-and-drop file list, and a hint bar
  at the bottom ("Drag to reorder · Click ✕ to remove")

Step 2 — Naming pattern:
- Two-column layout: naming pattern input + "Start from" number selector
- Start number defaults to 1, can be set to any number (0-9999)
- Live preview shows actual names with the start number:
  "Pattern 5, Pattern 6, Pattern 7… (12 total, ending at #16)"
- File list badges update to show the actual episode numbers

Step 3 — Details (optional):
- Speaker and folder selection in a grid

The start number is useful when continuing an existing series — if
episodes 1-10 already exist, set start to 11 and upload the next batch.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
