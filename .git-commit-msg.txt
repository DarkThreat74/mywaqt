perf: seamless tab switching — remove loading spinners for client pages

The top-level (app)/loading.tsx showed a full-page spinner on every
tab switch, even for pages that are pure client components with no
server-side data fetching. This made every navigation feel janky.

- Remove (app)/loading.tsx (was the catch-all spinner on every route)
- Remove loading.tsx for client-only pages: prayer, dhikr, qibla, talks
  (these pages have no async server work — no loading state needed)
- Replace remaining loading spinners (calendar/day, calendar/month,
  goals, settings) with skeleton layouts that match their page structure
  so the transition is smooth, not a jarring full-page flash

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
