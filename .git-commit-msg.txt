feat: admin settings, talks search, audio processing pipeline, media player iOS improvements

- Admin portal: add Settings tab with theme (light/dark/system), feature toggles,
  and audio processing configuration
- Admin users: show all users (not just role=user), fixed pagination bug
- Desktop sidebar: add Logout button at bottom-left
- Talks library: add search bar (by title, speaker, topic, description, folder)
  with debounced client-side filtering, works offline
- Talks admin: folder metadata (image, description, optional dates), topics field,
  processing status badges (pending/processing/ready/published/failed)
- Talks admin: upload → folder → metadata → process → publish workflow
- Audio processing: FFmpeg pipeline with silence removal, loudness normalization
  (loudnorm to -16 LUFS), noise reduction (afftdn), dynamic normalization
- Audio processing: runs in background via after(), maxDuration=300s
- Media player: iOS audio unlock, navigator.audioSession.type=playback,
  expanded MediaSession artwork, background track-end fallback interval
- Schema: migration 0019 adds folder image_key/start_date/end_date,
  talks processing_status/processed_storage_key/topics/published_at
- User-facing talks API now only returns published talks, prefers processed audio

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
