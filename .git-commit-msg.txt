feat: redesign admin forms — segmented control, mutual exclusivity, better UI

Replaced three separate toggle buttons with a segmented control bar.
Only one form can be open at a time — opening one closes the others.

UI improvements across all forms:
- Segmented control button bar (New Folder | Upload Talk | Batch Folder)
  Active button is filled black, inactive are transparent
- Each form now has:
  - Icon badge + title header with close (✕) button
  - Consistent rounded-2xl card with subtle shadow
  - Better spacing (gap-5, p-6)
  - Primary buttons with active:scale-95 press feedback
  - Upload progress shown as inline alert (not just text)
- Edit Folder form redesigned:
  - Icon badge with pencil icon
  - Folder name shown as subtitle under "Edit Folder" heading
  - Same card style as other forms
- Removed unused showFolderForm/showUploadForm/showBatchForm state
  (replaced by single activeForm: "folder" | "upload" | "batch" | null)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
