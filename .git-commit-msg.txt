feat: Waqt header links to calendar + premium Media Session notification

1. Waqt header text now links to /calendar
   Both mobile top bar and desktop sidebar "Waqt" text are now clickable
   links that navigate back to the calendar day view.

2. Redesigned Media Session notification for iOS + Android
   Deep research into Media Session API best practices applied:

   Artwork:
   - Folder image provided at 6 sizes (512/384/256/192/128/96) so both
     iOS and Android can pick the optimal resolution
   - MIME type auto-detected from URL extension (png/jpeg/webp/svg)
   - Highest-quality image first (iOS pre-18 picks the first element)
   - App icons (512/192 + maskable variants) always included as fallback
   - Never shows a generic/missing artwork icon

   Metadata:
   - title: talk title
   - artist: speaker, or folder name, or empty (never "Unknown")
   - album: folder name, or "Waqt Talks"
   - Set BEFORE play to avoid "unknown" metadata on some platforms

   Position state:
   - Updated on loadedmetadata, play, seeked, ratechange, and a
     throttled 1s interval during playback (OS extrapolates between)
   - Proper guards: duration must be positive/finite, playbackRate > 0,
     position clamped to [0, duration]
   - seekto uses fastSeek() when available for smooth scrubbing

   Playback state:
   - "playing" on play, "paused" on pause, "none" on stop/close
   - Full cleanup on unmount: metadata cleared, playbackState = "none",
     all action handlers removed, position state reset

   iOS-specific:
   - navigator.audioSession.type = 'playback' for background audio
   - playsInline + crossOrigin on audio element
   - Artwork sizes array for iOS 18 sizes-aware selection

   Android-specific:
   - Multiple artwork sizes for Chrome's 512/256 px ideal sizes
   - setPositionState for notification seek bar
   - All action handlers registered (play/pause/stop/seek/skip)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
