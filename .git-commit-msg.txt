feat: redesign edit folder UI, add folder speaker inheritance

Edit Folder form redesigned:
- Folder name is now editable (was missing entirely)
- Description field
- "Optional" section in a subtle bordered card containing:
  - Default speaker field — talks uploaded to this folder with no
    speaker automatically inherit this name
  - Compact date range (Start → End) in a single row, smaller inputs
  - Image upload as a clickable button (not raw file input):
    - Empty: "Click to upload an image"
    - Set: "Image set — click to replace" (green text)
    - Selected: shows filename + size

New Folder form also gets the default speaker field.

Schema: added `speaker` column to `talk_folders` table.
Migration: drizzle/0020_talk_folders_speaker.sql

API changes:
- create-folder: accepts optional `speaker`
- update-folder: accepts `name` (was not editable before) and `speaker`
- create-talk: when talk speaker is empty and folder has a speaker,
  inherits the folder's speaker automatically

⚠️ Run migration 0020 against Neon before deploying:
  drizzle/0020_talk_folders_speaker.sql

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
