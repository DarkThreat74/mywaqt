fix: mini player overlapping bottom nav + FAB/keyboard view cycling

Layout fixes:
- Mini player was at bottom:0 z-50, overlapping the bottom nav (z-40).
  Now sits at bottom: calc(4rem + safe-area) on mobile (above the nav),
  and at the bottom on desktop (lg: class overrides to bottom:0).
- FAB was at bottom: 5rem on all viewports — too high on desktop where
  there's no bottom nav. Now uses lg: class to sit at 1.5rem on desktop.
- CSS --player-bar-h was set to 0px on desktop, but the mini bar still
  appears on desktop. Now keeps 64px on desktop so content padding works.
- Removed overly complex lg: positioning classes on mini bar (simplified
  to a single lg:bottom override).

Keyboard fix:
- "f" shortcut toggled between full and mini only, ignoring the new fab
  state. Now cycles fab → mini → full → fab.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
