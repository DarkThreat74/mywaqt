fix: correct for Vercel Hobby plan limits (300s, 1024MB)

User is on Vercel Hobby (free) plan, not Pro. Previous settings
(maxDuration=800, memory=3009) were being silently capped by Vercel
to Hobby limits, causing processing to fail.

Hobby plan limits:
- Max duration: 300s (5 min)
- Max memory: 2GB (we use 1024MB — sufficient for transcoding)
- /tmp: 512MB

Tiered processing adjusted for 300s timeout:
  <25 MB:   Full processing (all filters) — ~1-3 min
  25-50 MB: Fast mode (skip expensive filters) — ~3-5 min
  50-100 MB: Ultra-fast (just transcode, no filters) — ~3-5 min
  >100 MB:  Skip processing — use original file directly

For files >100MB: The talk is marked as "ready" immediately with
processedStorageKey = storageKey (original file). The talk still
plays and downloads normally — it's just not compressed. This is
the only reliable option on the Hobby plan. The API returns a clear
message explaining this and suggesting Pro upgrade for compression.

Config fixes:
- maxDuration: 300 (Hobby max, was 800)
- memory: 1024 (was 3009)
- Stale detection: 300s (was 800s)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
