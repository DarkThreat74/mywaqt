perf: 100k-user scalability + auth hardening

- AlAdhan location dedup: 100k users to ~1k API calls via (lat,lng,method,madhab,month,year) grouping
- Prayer-times sync staleness fix: query prayer_times_cache instead of comparing month strings
- DB-backed per-account brute-force lockout (10 failures/15min, survives cold starts)
- Trusted device limit (10/user) + stale device cleanup (90 days unused)
- IndexedDB cache pruning: events/prayer logs/prayer times/sunnah/habit logs >90 days
- Cron push parallelization: 10 concurrent instead of sequential (2.8h to 17min)
- Offline outbox idempotency keys to prevent double-apply on replay
- Daily cron cleanup: prune login_attempts >24h + trusted_devices >90 days
- Migration 0024: login_attempts table + index

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
