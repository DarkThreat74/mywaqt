feat: advanced media player with 15+ premium features

Replaces the basic audio player with a full-featured player component:

- Mini-player bar (bottom) with progress, play/pause, skip 15/30s
- Full-player bottom sheet with large artwork and all controls
- Media Session API: full action set (play, pause, stop, seekbackward,
  seekforward, seekto, previoustrack, nexttrack), position state,
  artwork, lock screen/notification controls
- Playback speed control: 0.5x to 2x with preservesPitch
- Sleep timer: 5min to 1hr, position-based with 30s fade-out
- Playback resume: saves position every 5s to localStorage, restores
  on next visit (cleared when track ends)
- Queue management: view queue, jump to any track, shuffle (Fisher-Yates)
- Repeat modes: off, all, one (repeat current track)
- Bookmarks: add/jump/delete, stored per-track in localStorage
- Volume control: perceptual curve, mute toggle, persisted
- Keyboard shortcuts: space/k (play), arrows (seek/volume),
  m (mute), n/p (next/prev), f (fullscreen)
- Double-tap to skip: tap left/right side of full player for -10/+10s
- Error handling: exponential backoff retry (3 attempts), error UI
- Buffering states: loading spinner, buffered progress indicator
- AirPlay: RemotePlayback API integration (shows button when available)
- Offline save/remove: Cache API with status indicator
- Animated equalizer bars in mini and full player when playing

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
