fix: improve talk/folder delete reliability and error visibility

Admin UI (AdminTalks.tsx):
- deleteTalk and deleteFolder now parse and show the actual API error
  message instead of a generic "Failed to delete talk." string
- Added try/catch for network errors so the user gets a clear message
  if the request never reaches the server

API route (api/admin/talks/route.ts):
- Replaced silent catch blocks on R2 deleteObject calls with logError()
  per AGENTS.md rule "No silent catch blocks — at minimum logError()"
- Covers: delete-talk (storageKey + processedStorageKey), delete-folder
  (imageKey), update-folder (old imageKey)

The delete flow itself was already correct — DB row is deleted first,
then R2 objects are removed best-effort. The improvements make failures
visible instead of silent.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
