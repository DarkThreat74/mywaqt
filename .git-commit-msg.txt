feat: global audio player survives route changes + SW audio caching

- Global audio player: moved from TalksClient to (app) layout via
  AudioPlayerProvider context. Player now persists across navigation —
  background playback continues when leaving /talks
- AudioPlayerContext: manages current track, queue, next/prev, offline status
- GlobalAudioPlayer: lazy-loads AdvancedAudioPlayer only when a track is active
- TalksClient simplified: uses context.play() instead of local state + rendering
- Service worker: now intercepts audio requests from R2 with cache-first
  strategy, talkId-based matching (presigned URLs change but talkId is stable),
  background revalidation, and 503 fallback when offline with no cache
- SW cache version bumped to v32, AUDIO_CACHE added to valid caches list
- All audio cache references updated from v31 to v32

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
